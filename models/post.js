module.exports = function(sequelize, DataTypes) {
    const Post = sequelize.define("Post", {
    //code block (string max)
    //user id (string)
    //tags 
    //post id (sequleize)
    });
    return Post;
}